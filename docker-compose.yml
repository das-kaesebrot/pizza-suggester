version: '2'

services:
  kantine-bot:
    build:
        context: .
    restart: on-failure
    environment:
        # INSERT YOUR BOT TOKEN HERE
        # - TGBOTTOKEN=
        # API base URL, defaults to https://api.telegram.org/bot
        # - API_BASEURL=