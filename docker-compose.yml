version: '2'

services:
  kantine-bot:
    build:
        context: .
    restart: always
    environment:
        # INSERT YOUR BOT TOKEN HERE
        # - TGBOTTOKEN=
        # INSERT YOUR HOOKS BASE URL HERE
        # - HOOKSBASEURL=https://example.com/hooks
        # API base URL, defaults to https://api.telegram.org/bot
        # - API_BASEURL=
    ports:
      - 127.0.0.1:8000:8000